// Example 23
// Template classes

#include <string>
#include <iostream>
#include "Accum.h"
using namespace std;

int main(int argc, char const *argv[]) {
  Accum<int> integrers(0);
  integers += 3;
  integers += 7;
  cout << integers.getTotal() << endl;
  return 0;
}
